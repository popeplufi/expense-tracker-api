export default function ChatPage() {
  return (
    <main className="shell">
      <section className="card">
        <h1>Chat Workspace</h1>
        <p>
          Build your realtime chat UI here using the typed contracts in
          <code> src/lib/contracts/api.ts </code>
          and encrypted local cache in
          <code> src/lib/storage/indexeddb.ts</code>.
        </p>
      </section>
    </main>
  );
}
